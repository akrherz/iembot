language: python

python:
  - "2.6"

install:
  - "pip install -q Twisted"
  - "pip install coveralls"
  - "pip install oauth"

script:
  "coverage run --source=iembot setup.py test"

after_success:
  coveralls